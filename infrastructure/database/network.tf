# This file is intentionally left minimal as networking resources 
# are managed externally in the Azure Landing Zone environment.
# The main resources reference existing VNet and subnets via data sources.

# Data source for existing virtual network is already defined in postgresql-flexible.tf
# Data source for existing database subnet is already defined in postgresql-flexible.tf